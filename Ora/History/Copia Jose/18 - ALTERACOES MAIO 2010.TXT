ALTER TABLE EXAMES MODIFY(OBSERVACOES VARCHAR2(500));

ALTER TABLE COMPLEMENTO_ATENDIMENTOS MODIFY(OBSERVACOES VARCHAR2(500));

ALTER TABLE GRUPOS_ETIQUETAS ADD(SEPARA_ETIQUETAS VARCHAR2(1));
UPDATE GRUPOS_ETIQUETAS SET SEPARA_ETIQUETAS = 'N';
ALTER TABLE GRUPOS_ETIQUETAS MODIFY SEPARA_ETIQUETAS NOT NULL;

CREATE OR REPLACE PROCEDURE PRC_GRUPOS_ETIQUETAS(V_GRUPO_ETIQUETA   IN OUT GRUPOS_ETIQUETAS.GRUPO_ETIQUETA%TYPE,
                                                 V_NOME             IN     GRUPOS_ETIQUETAS.NOME%TYPE,
                                                 V_MATERIAL         IN     GRUPOS_ETIQUETAS.MATERIAL%TYPE,
                                                 V_QT_ETIQUETAS     IN     GRUPOS_ETIQUETAS.QT_ETIQUETAS%TYPE,
                                                 V_SEPARA_ETIQUETAS IN     GRUPOS_ETIQUETAS.SEPARA_ETIQUETAS%TYPE,
                                                 V_FUNCAO           IN     NUMBER) IS
BEGIN
  IF V_FUNCAO = 1 THEN
     SELECT SQ_GRUPO_ETIQUETA.NEXTVAL INTO V_GRUPO_ETIQUETA FROM DUAL;
     INSERT INTO GRUPOS_ETIQUETAS VALUES (V_GRUPO_ETIQUETA, V_NOME, V_MATERIAL, V_QT_ETIQUETAS, V_SEPARA_ETIQUETAS);

  ELSIF V_FUNCAO = 2 THEN
     UPDATE GRUPOS_ETIQUETAS SET NOME = V_NOME, MATERIAL = V_MATERIAL, QT_ETIQUETAS = V_QT_ETIQUETAS, SEPARA_ETIQUETAS = V_SEPARA_ETIQUETAS
      WHERE GRUPO_ETIQUETA = V_GRUPO_ETIQUETA;

  ELSIF V_FUNCAO = 3 THEN
     DELETE GRUPOS_ETIQUETAS WHERE GRUPO_ETIQUETA = V_GRUPO_ETIQUETA;
  END IF;

  COMMIT WORK;

END PRC_GRUPOS_ETIQUETAS;
/

ALTER TABLE EVOLUCAO_RESULTADOS DROP COLUMN EVOLUCAO_RESULTADO;
ALTER TABLE EVOLUCAO_RESULTADOS ADD(USUARIO_EVOLUCAO VARCHAR2(10) NOT NULL);
ALTER TABLE EVOLUCAO_RESULTADOS ADD(EVOLUCAO_RESULTADO VARCHAR2(200) NOT NULL);

CREATE OR REPLACE PROCEDURE PRC_EVOLUCAO_RESULTADOS(V_POSTO              IN EVOLUCAO_RESULTADOS.POSTO%TYPE, 
                                             	    V_ATENDIMENTO        IN EVOLUCAO_RESULTADOS.ATENDIMENTO%TYPE, 
                                           	    V_CORREL             IN EVOLUCAO_RESULTADOS.CORREL%TYPE, 
                                           	    V_DATA_EVOLUCAO      IN EVOLUCAO_RESULTADOS.DATA_EVOLUCAO%TYPE, 
                                           	    V_USUARIO_EVOLUCAO   IN EVOLUCAO_RESULTADOS.USUARIO_EVOLUCAO%TYPE, 
                                           	    V_EVOLUCAO_RESULTADO IN EVOLUCAO_RESULTADOS.EVOLUCAO_RESULTADO%TYPE, 
                                                    V_FUNCAO             IN NUMBER) IS
BEGIN
  IF V_FUNCAO = 1 THEN
     INSERT INTO EVOLUCAO_RESULTADOS VALUES (V_POSTO, V_ATENDIMENTO, V_CORREL, V_DATA_EVOLUCAO, V_USUARIO_EVOLUCAO, V_EVOLUCAO_RESULTADO);

  ELSIF V_FUNCAO = 2 THEN
     UPDATE EVOLUCAO_RESULTADOS SET EVOLUCAO_RESULTADO = V_EVOLUCAO_RESULTADO
                        WHERE POSTO = V_POSTO AND ATENDIMENTO = V_ATENDIMENTO AND CORREL = V_CORREL AND DATA_EVOLUCAO = V_DATA_EVOLUCAO;
  ELSIF V_FUNCAO = 3 THEN
     DELETE EVOLUCAO_RESULTADOS WHERE POSTO = V_POSTO AND ATENDIMENTO = V_ATENDIMENTO AND CORREL = V_CORREL AND DATA_EVOLUCAO = V_DATA_EVOLUCAO;

  END IF;

  COMMIT WORK;

END PRC_EVOLUCAO_RESULTADOS;
/
