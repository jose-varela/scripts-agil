-- View: LIS.CONVENIOS_WEB

-- DROP VIEW LIS.CONVENIOS_WEB;

CREATE VIEW LIS.CONVENIOS_WEB
(
  CONVENIO,
  NOME,
  STATUS
)
AS
SELECT CONVENIO, NOME, STATUS
  FROM CONVENIOS
  WHERE STATUS = 'A'
    AND NOME_FICHA IS NOT NULL
ORDER BY CONVENIO;

GRANT SELECT
 ON LIS.CONVENIOS_WEB
TO WLIS;
