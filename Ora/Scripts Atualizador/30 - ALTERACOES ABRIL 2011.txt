CREATE OR REPLACE PROCEDURE PRC_CAIXA(V_CONTA_HOSP     IN CAIXA.CONTA_HOSP%TYPE,
                                      V_REGISTRO       IN CAIXA.REGISTRO%TYPE,
                                      V_CONTA          IN CAIXA.CONTA%TYPE,
                                      V_DATA_PAG       IN CAIXA.DATA_PAG%TYPE,
                                      V_VALOR          IN CAIXA.VALOR%TYPE,
                                      V_DESCONTO       IN CAIXA.DESCONTO%TYPE,
                                      V_TIPO_DTO       IN CAIXA.TIPO_DTO%TYPE,
                                      V_USUARIO        IN CAIXA.USUARIO%TYPE,
                                      V_DATA_CAD       IN CAIXA.DATA_CAD%TYPE,
                                      V_CORREL         IN OUT CAIXA.CORREL%TYPE,
                                      V_TIPO_PAG       IN CAIXA.TIPO_PAG%TYPE,
                                      V_CATALOGO       IN CAIXA.CATALOGO%TYPE,
                                      V_FUNCAO         IN NUMBER) IS
BEGIN
  IF V_FUNCAO = 1 THEN
     SELECT NVL(MAX(C.CORREL)+1,1) INTO V_CORREL FROM CAIXA C 
      WHERE C.CONTA_HOSP = V_CONTA_HOSP;

     INSERT INTO CAIXA VALUES (V_CONTA_HOSP,  V_REGISTRO,  V_CONTA,     V_DATA_PAG,
                                     V_VALOR,       V_DESCONTO,  V_TIPO_DTO,  V_USUARIO,
                                     V_DATA_CAD,    V_CORREL,    V_TIPO_PAG,  V_CATALOGO);

  ELSIF V_FUNCAO = 2 THEN
     UPDATE CAIXA SET VALOR = V_VALOR
                      WHERE CONTA_HOSP = V_CONTA_HOSP AND REGISTRO = V_REGISTRO AND CORREL = V_CORREL;

  ELSIF V_FUNCAO = 3 THEN
     DELETE CAIXA WHERE CONTA_HOSP = V_CONTA_HOSP;

  ELSIF V_FUNCAO = 4 THEN
     DELETE CAIXA WHERE CONTA_HOSP = V_CONTA_HOSP AND REGISTRO = V_REGISTRO AND CORREL = V_CORREL;

  ELSIF V_FUNCAO = 5 THEN
     UPDATE CAIXA SET VALOR = V_VALOR, DATA_PAG = V_DATA_PAG, DESCONTO = V_DESCONTO, TIPO_DTO = V_TIPO_DTO, TIPO_PAG = V_TIPO_PAG
                      WHERE CONTA_HOSP = V_CONTA_HOSP AND REGISTRO = V_REGISTRO AND CORREL = V_CORREL;
  END IF;

  COMMIT WORK;
END PRC_CAIXA;
-- fim bloco

-- tabela BIOQUIMICOS_SETORES

ALTER TABLE BIOQUIMICOS_SETORES ADD(RESPONSAVEL VARCHAR2(1));
-- fim bloco
UPDATE BIOQUIMICOS_SETORES SET RESPONSAVEL = 'N';
-- fim bloco
ALTER TABLE BIOQUIMICOS_SETORES MODIFY RESPONSAVEL NOT NULL;
-- fim bloco

CREATE OR REPLACE PROCEDURE PRC_BIOQUIMICOS_SETORES(V_USER_CODIGO    IN OUT BIOQUIMICOS_SETORES.USER_CODIGO%TYPE,
                                                    V_SETOR          IN     BIOQUIMICOS_SETORES.SETOR%TYPE,
                                                    V_RESPONSAVEL    IN     BIOQUIMICOS_SETORES.RESPONSAVEL%TYPE,
                                                    V_FUNCAO         IN     NUMBER) IS
BEGIN
  IF V_FUNCAO = 1 THEN
     INSERT INTO BIOQUIMICOS_SETORES VALUES (V_USER_CODIGO, V_SETOR, V_RESPONSAVEL);

  ELSIF V_FUNCAO = 2 THEN
     UPDATE BIOQUIMICOS_SETORES SET RESPONSAVEL = V_RESPONSAVEL WHERE USER_CODIGO = V_USER_CODIGO AND SETOR = V_SETOR;

  ELSIF V_FUNCAO = 3 THEN
     DELETE BIOQUIMICOS_SETORES WHERE USER_CODIGO = V_USER_CODIGO AND SETOR = V_SETOR ;

  ELSIF V_FUNCAO = 4 THEN
     DELETE BIOQUIMICOS_SETORES WHERE USER_CODIGO = V_USER_CODIGO;
     
  END IF;           

  COMMIT WORK;

END PRC_BIOQUIMICOS_SETORES;
-- fim bloco



CREATE INDEX EXAMES_B2B_LOTE_I    
       ON EXAMES_B2B (LOTE_B2B)
       TABLESPACE LIS_INDX;
-- fim bloco
