CREATE OR REPLACE VIEW VEX_POSTOS
(
  POSTO,
  NOME,
  INTERNO,
  REALIZA_EXAMES,
  TIPO_POSTO,
  PASS,
  STATUS
)
AS
SELECT POSTO, NOME, INTERNO, REALIZA_EXAMES, TIPO_POSTO, PASS, STATUS
  FROM POSTOS
  WHERE STATUS = 'A'
ORDER BY POSTO;

GRANT SELECT
 ON VEX_POSTOS
TO EXPERIENCE;


