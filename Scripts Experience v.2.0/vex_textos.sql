CREATE OR REPLACE VIEW VEX_TEXTOS
(
  MNEMONICO,
  NUMERO,
  DESCRICAO,
  TEXTO,
  TIPO,
  STATUS
)
AS
SELECT MNEMONICO, NUMERO, DESCRICAO, TEXTO, TIPO, STATUS
  FROM TEXTOS
ORDER BY MNEMONICO, NUMERO;

GRANT SELECT
 ON VEX_TEXTOS
TO EXPERIENCE;
