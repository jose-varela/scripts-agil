CREATE OR REPLACE VIEW VEX_PREPAROS
(
  MNEMONICO,
  NOME_EXAME,
  PREPARO
)
AS
SELECT 
	P.MNEMONICO, 
	PR.NOME,
	P.PREPARO
FROM 
	PREPAROS P,
	PROCEDIMENTOS PR
WHERE
	P.MNEMONICO = PR.MNEMONICO AND
    PR.STATUS = 'A';