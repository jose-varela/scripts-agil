CREATE OR REPLACE VIEW VEX_VARIAVEIS
(
  VARIAVEL,
  NOME,
  UNIDADE,
  DECIMAIS,
  ORDEM_VISUALIZACAO
)
AS
SELECT VARIAVEL, NOME, UNIDADE, DECIMAIS, ORDEM_VISUALIZACAO
  FROM VARIAVEIS
ORDER BY VARIAVEL;

GRANT SELECT
 ON VEX_VARIAVEIS
TO EXP_CLB;

