CREATE TABLE ATENDIMENTOS
(POSTO         SMALLINT     NOT NULL,
 REGISTRO      INT     NOT NULL,
 ATENDIMENTO   INT,
 CONVENIO      SMALLINT     NOT NULL,
 MATRICULA     VARCHAR(23),
 DATA_ATD      TIMESTAMP(0)          NOT NULL, 
 DATA_FAT      TIMESTAMP(0)          NOT NULL,
 DATA_ENT      TIMESTAMP(0),
 HORA_ENT      VARCHAR(5),
 MED_SOL       VARCHAR(9)   NOT NULL,
 LIB_FAT       VARCHAR(1)   NOT NULL,
 FATURAMENTO   VARCHAR(1)   NOT NULL,
 USUARIO       VARCHAR(10)  NOT NULL,
 DATA_CAD      TIMESTAMP(0),
 POSTO_ENTREGA SMALLINT     NOT NULL,
 SITUACAO      VARCHAR(1)   NOT NULL,
 CODIGO_PLANO  DECIMAL(30),
 DATA_VALIDADE TIMESTAMP(0),
 ID_FATURA     INT)
;
