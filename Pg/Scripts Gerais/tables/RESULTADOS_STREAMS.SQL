CREATE TABLE RESULTADOS_STREAMS
(POSTO           SMALLINT    NOT NULL,
 ATENDIMENTO     INT    NOT NULL, 
 CORREL          SMALLINT    NOT NULL,
 NUMERO          SMALLINT    NOT NULL,
 VARIAVEL        INT    NOT NULL,
 STREAM          BYTEA,
 USUARIO_AUTO    VARCHAR(10) NOT NULL,
 DATA_AUTO       TIMESTAMP(0)         NOT NULL,
 ESTACAO_AUTO    VARCHAR(20))
;
