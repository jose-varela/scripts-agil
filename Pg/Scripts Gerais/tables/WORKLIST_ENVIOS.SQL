CREATE TABLE WORKLIST_ENVIOS
(POSTO           SMALLINT     NOT NULL,
 ATENDIMENTO     INT     NOT NULL,
 ETIQUETA        SMALLINT     NOT NULL,
 EQUIPAMENTO     SMALLINT     NOT NULL,
 DATA_CAD        TIMESTAMP(0)          NOT NULL,
 USUARIO         VARCHAR(10) NOT NULL,
 SITUACAO_ENVIO  VARCHAR(1)  NOT NULL)
;

