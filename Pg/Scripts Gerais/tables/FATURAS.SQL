CREATE TABLE FATURAS
(CONVENIO     SMALLINT,
 DATA_DESDE   TIMESTAMP(0)         NOT NULL,
 DATA_ATE     TIMESTAMP(0)         NOT NULL,
 FECHADA      VARCHAR(1)  NOT NULL,
 DISKETE      VARCHAR(1)  NOT NULL,
 STATUS       VARCHAR(1)  ,
 USUARIO      VARCHAR(10) NOT NULL,
 DATA_CAD     TIMESTAMP(0)         NOT NULL,
 VALOR_FAT    DECIMAL(10,2),
 VALOR_PAG    DECIMAL(10,2),
 VALOR_GLO    DECIMAL(10,2),
 ID_FATURA    INT NOT NULL,
 CODIGO_PLANO SMALLINT)
;
