CREATE TABLE CAIXA
(CONTA_HOSP      INT,
 REGISTRO        INT     NOT NULL,
 CONTA           SMALLINT     NOT NULL,
 DATA_PAG        TIMESTAMP(0)          NOT NULL,
 VALOR           DECIMAL(10,2)  NOT NULL,
 DESCONTO        DECIMAL(10,2),
 TIPO_DTO        VARCHAR(1),
 USUARIO         VARCHAR(10)  NOT NULL,
 DATA_CAD        TIMESTAMP(0)          NOT NULL,
 CORREL          SMALLINT     NOT NULL,
 TIPO_PAG        VARCHAR(2)   NOT NULL,
 CATALOGO        SMALLINT     NOT NULL,
 USUARIO_DTO     VARCHAR(10),
 USUARIO_ALT     VARCHAR(10),
 DATA_ALT        TIMESTAMP(0)
)
;
