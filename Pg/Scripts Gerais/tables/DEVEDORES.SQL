CREATE TABLE DEVEDORES
(CONTA_HOSP      INT,
 REGISTRO        INT    NOT NULL,
 VALOR_FAT       DECIMAL(10,2) NOT NULL,
 VALOR           DECIMAL(10,2) NOT NULL,
 VALOR_PAG       DECIMAL(10,2) NOT NULL,
 VALOR_DTO       DECIMAL(10,2) NOT NULL,
 DATA_CAD        TIMESTAMP(0)         NOT NULL,
 CATALOGO        SMALLINT    NOT NULL,
 STATUS          VARCHAR(1)  NOT NULL,
 USUARIO_STATUS  VARCHAR(10),
 DATA_STATUS     VARCHAR(10))
;
