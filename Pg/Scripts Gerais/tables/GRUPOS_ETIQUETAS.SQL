CREATE TABLE GRUPOS_ETIQUETAS
(GRUPO_ETIQUETA          INT NOT NULL,
 NOME                    VARCHAR(50),
 MATERIAL                SMALLINT NOT NULL,
 QT_ETIQUETAS            SMALLINT NOT NULL,
 SEPARA_ETIQUETAS        VARCHAR(1) NOT NULL,
 TIPO_CODIGO             VARCHAR(20) NOT NULL,
 MARGEM_ESQUERDA_CODIGO  SMALLINT NOT NULL,
 RATIO_CODIGO            SMALLINT NOT NULL)
;
