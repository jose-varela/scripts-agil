CREATE TABLE PLANOS_CONVENIOS
(CONVENIO     SMALLINT     NOT NULL,
 CODIGO_PLANO SMALLINT     NOT NULL,
 PLANO        VARCHAR(30)  NOT NULL,
 STATUS       VARCHAR(1)   NOT NULL,
 DATA_CAD     TIMESTAMP(0)          NOT NULL,
 USUARIO      VARCHAR(10)  NOT NULL)
;
