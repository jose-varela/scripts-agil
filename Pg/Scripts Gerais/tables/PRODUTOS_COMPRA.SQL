CREATE TABLE PRODUTOS_COMPRA
(NUMERO           INT,
 PRODUTO_ID       SMALLINT,
 QTD              BIGINT   NOT NULL,
 VALOR            DECIMAL(10,2) NOT NULL,
 TIPO_DESCONTO    VARCHAR(1),
 DESCONTO         DECIMAL(10,2),
 PRAZO_ENTREGA    SMALLINT    NOT NULL,
 QTD_ENTREGUE     BIGINT,
 IPI			DECIMAL(5,2))
;
